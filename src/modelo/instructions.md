Rules for editing code:

- Modify only the necessary lines.
- Do not move functions.
- Do not reorder code in the file.
- Do not refactor unless explicitly requested.
- Keep the existing structure of the file.
- If you want to do things I did not ask for, ask first.
- If you need to add a line (for example in CSS), do not delete three lines and add a fourth. Simply add the new line below so I don’t have to review many differences; I will organize it later.
